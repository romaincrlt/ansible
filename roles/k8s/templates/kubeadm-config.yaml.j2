apiVersion: kubeadm.k8s.io/v1beta3
kind: ClusterConfiguration
kubernetesVersion: {{ kube_version }}
controlPlaneEndpoint: "{{ first_control_plane + ':6443' }}"
networking:
  podSubnet: {{ pod_network_cidr }}
