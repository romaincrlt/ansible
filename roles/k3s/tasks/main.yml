---
- name: Download K3s install script
  ansible.builtin.get_url:
    url: https://get.k3s.io/
    timeout: 120
    dest: "/home/{{ ansible_user }}/k3s-install.sh"
    mode: "0755"
  register: download_k3s_script

- name: Ensure k3s directory exist
  become: true
  ansible.builtin.file:
    path: /etc/rancher/k3s
    state: directory
    mode: "0755"

- name: Create k3s config file
  become: true
  ansible.builtin.template:
    src: templates/k3s-config.yaml.j2
    dest: /etc/rancher/k3s/config.yaml
    owner: "root"
    mode: 0644
  register: edit_k3s_config_file

- name: Execute K3s install script
  become: true
  ansible.builtin.command:
    cmd: "/home/{{ ansible_user }}/k3s-install.sh"
    creates: "/etc/rancher/k3s/k3s.yaml"
  register: k3s_first_install

- name: Upgrade K3s
  become: true
  when: download_k3s_script.changed and k3s_upgrade_if_necessary == true and not k3s_first_install.changed
  ansible.builtin.command:
    cmd: "/home/{{ ansible_user }}/k3s-install.sh"

- name: Restart K3s service
  become: true
  when: edit_k3s_config_file.changed and not k3s_first_install.changed
  ansible.builtin.systemd_service:
    name: k3s.service
    scope: system
    state: restarted

- name: Enable Gateway API
  import_tasks: enable_gateway_api.yml
  when: k3s_enable_gateway_api
